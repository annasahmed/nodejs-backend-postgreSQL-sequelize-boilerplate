const Joi = require('@hapi/joi');

const createPlace = {
	body: Joi.object().keys({
		title: Joi.string().required(),
		slug: Joi.string().required(),
		excerpt: Joi.string().required(),
		about: Joi.string().required(),
		contact: Joi.string().required(),
		website: Joi.string().uri().required(),
		instagram: Joi.string().uri().required(),
		hotel: Joi.string(),
		bookingUrl: Joi.string().uri().required(),
		location: Joi.string().uri().required(),
		latitude: Joi.number().required(),
		longitude: Joi.number().required(),
		address: Joi.string().required(),
		emirateId: Joi.number().required(),
		areaId: Joi.number().required(),
		trending: Joi.boolean().default(false),
		email: Joi.string().email(),
		status: Joi.boolean().default(true),
		logo: Joi.any(),
		menu: Joi.string().uri(),
		featured: Joi.any(),
		start_date: Joi.date(),
		reel: Joi.any(),
		userId: Joi.number().required(),
		subscriptionId: Joi.number().required(),
		subscriptionStatusId: Joi.number().required(),
		dealIds: Joi.array().items(Joi.number()),
		categoryIds: Joi.array().items(Joi.number()),
		cuisineIds: Joi.array().items(Joi.number()),
		uspIds: Joi.array().items(Joi.number()),
		happenings: Joi.array().items(Joi.object()),
		timings: Joi.array().items(Joi.object()),
		isCommission: Joi.boolean().default(false),
		commission: Joi.number().default(0.0),
		iframe: Joi.string(),
	}),
};
const updatePlace = {
	body: Joi.object().keys({
		title: Joi.string(),
		slug: Joi.string(),
		excerpt: Joi.string(),
		about: Joi.string(),
		contact: Joi.string(),
		website: Joi.string().uri(),
		instagram: Joi.string().uri(),
		hotel: Joi.string(),
		bookingUrl: Joi.string().uri(),
		location: Joi.string().uri(),
		latitude: Joi.number(),
		longitude: Joi.number(),
		address: Joi.string(),
		emirateId: Joi.number(),
		areaId: Joi.number(),
		trending: Joi.boolean().default(false),
		email: Joi.string().email(),
		status: Joi.boolean().default(true),
		logo: Joi.any(),
		menu: Joi.string().uri(),
		featured: Joi.any(),
		start_date: Joi.date(),
		reel: Joi.any(),
		userId: Joi.number(),
		subscriptionId: Joi.number(),
		subscriptionStatusId: Joi.number(),
		dealIds: Joi.array().items(Joi.number()),
		categoryIds: Joi.array().items(Joi.number()),
		cuisineIds: Joi.array().items(Joi.number()),
		uspIds: Joi.array().items(Joi.number()),
		happenings: Joi.array().items(Joi.object()),
		timings: Joi.array().items(Joi.object()),
		isCommission: Joi.boolean().default(false),
		commission: Joi.number().default(0.0),
		iframe: Joi.string(),
	}),
};

module.exports = {
	createPlace,
	updatePlace,
};
